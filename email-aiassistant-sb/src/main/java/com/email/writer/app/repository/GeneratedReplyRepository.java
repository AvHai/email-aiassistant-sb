package com.email.writer.app.repository;

import com.email.writer.app.entity.GeneratedReply;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GeneratedReplyRepository extends JpaRepository<GeneratedReply, Long> {
}
